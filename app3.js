// condicionais if e else
// aplicaque os dewscontos de acordo com o valor da compra
// acima de 100 -> 15%
// entre 500 e 1000 -> 10%
// abaixo de 500 -> 5%

let to = 50.0000;
console.log(to.toFixed(2))
let oi = prompt("uo")
console.log(oi)